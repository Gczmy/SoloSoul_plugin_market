[package]
name = "packing-list"
version = "1.0.0"
edition = "2021"
authors = ["SoloSoul Official"]
description = "Packing List Generator — Generate travel packing lists based on destination and season"

[dependencies]
solosoul-plugin-sdk = { path = "../../SDK/rust" }

[lib]
crate-type = ["cdylib"]
