[package]
name = "phone-fmt"
version = "1.0.0"
edition = "2021"
authors = ["SoloSoul Official"]
description = "Phone Formatter — Format phone numbers according to country conventions"

[dependencies]
solosoul-plugin-sdk = { path = "../../SDK/rust" }

[lib]
crate-type = ["cdylib"]
