[package]
name = "form-prefiller"
version = "1.0.0"
edition = "2021"
authors = ["SoloSoul Official"]
description = "Form Prefiller — Generate field mapping tables for common forms"

[dependencies]
solosoul-plugin-sdk = { path = "../../SDK/rust" }

[lib]
crate-type = ["cdylib"]
