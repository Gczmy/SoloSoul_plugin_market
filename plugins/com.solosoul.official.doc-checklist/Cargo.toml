[package]
name = "doc-checklist"
version = "1.0.0"
edition = "2021"
authors = ["SoloSoul Official"]
description = "Doc Checklist — Check required documents for visa/travel/bank scenarios"

[dependencies]
solosoul-plugin-sdk = { path = "../../SDK/rust" }

[lib]
crate-type = ["cdylib"]
