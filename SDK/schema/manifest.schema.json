{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://plugins.solosoul.dev/schema/manifest.schema.json",
  "title": "SoloSoul Plugin Manifest",
  "description": "插件清单规范，每个插件必须包含 manifest.json",
  "type": "object",
  "required": [
    "plugin_id",
    "name",
    "version",
    "plugin_api_version",
    "min_app_version",
    "max_app_version",
    "description",
    "publisher",
    "required_fields"
  ],
  "properties": {
    "plugin_id": {
      "type": "string",
      "description": "反向域名格式，全局唯一标识",
      "pattern": "^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)+$",
      "examples": ["com.solosoul.slotgo"]
    },
    "name": {
      "type": "string",
      "description": "插件显示名称",
      "minLength": 1,
      "maxLength": 64
    },
    "version": {
      "type": "string",
      "description": "SemVer 格式",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-([a-zA-Z0-9-]+(?:\\.[a-zA-Z0-9-]+)*))?(?:\\+([a-zA-Z0-9-]+(?:\\.[a-zA-Z0-9-]+)*))?$"
    },
    "plugin_api_version": {
      "type": "string",
      "description": "插件 ABI 版本，与主软件严格匹配",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$",
      "examples": ["1.0", "1.1"]
    },
    "min_app_version": {
      "type": "string",
      "description": "兼容的最低 SoloSoul App 版本",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$"
    },
    "max_app_version": {
      "type": "string",
      "description": "兼容的最高 SoloSoul App 版本",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$"
    },
    "description": {
      "type": "string",
      "description": "一句话描述插件功能",
      "minLength": 1,
      "maxLength": 256
    },
    "publisher": {
      "type": "string",
      "description": "发布者名称",
      "minLength": 1,
      "maxLength": 64
    },
    "homepage": {
      "type": "string",
      "description": "项目主页 URL",
      "format": "uri"
    },
    "signature": {
      "type": "string",
      "description": "Ed25519 签名（Release 模式强制校验）"
    },
    "required_fields": {
      "type": "array",
      "description": "插件必需的字段路径列表",
      "minItems": 1,
      "items": {
        "type": "string",
        "description": "UnifiedObject 字段路径，支持通配符",
        "examples": [
          "identity.full_name",
          "travel.*"
        ]
      }
    },
    "optional_fields": {
      "type": "array",
      "description": "可选字段路径列表",
      "items": {
        "type": "string"
      }
    },
    "network_policy": {
      "type": "object",
      "description": "网络白名单策略",
      "required": ["block_all_outbound", "allowed_domains"],
      "properties": {
        "block_all_outbound": {
          "type": "boolean",
          "description": "是否阻止所有出站访问（默认 true）",
          "default": true
        },
        "allowed_domains": {
          "type": "array",
          "description": "允许的域名列表，支持通配符",
          "items": {
            "type": "string",
            "examples": ["*.solosoul.dev", "api.example.com"]
          }
        }
      }
    },
    "data_ttl_seconds": {
      "type": "integer",
      "description": "敏感数据内存存活时间（秒）",
      "minimum": 30,
      "maximum": 3600,
      "default": 300
    },
    "require_user_confirmation": {
      "type": "boolean",
      "description": "是否要求用户逐字段确认",
      "default": true
    },
    "consent_validity_hours": {
      "type": "integer",
      "description": "授权有效期（小时）",
      "minimum": 1,
      "maximum": 168,
      "default": 24
    },
    "contracts": {
      "type": "array",
      "description": "插件服务的契约类型。Stage 4 typed-lookup 的入口锚点。",
      "items": {
        "type": "object",
        "required": ["typeId"],
        "properties": {
          "typeId": {
            "type": "string",
            "description": "反向域名+主版本，匹配 UserTemplate.contract_type_id",
            "pattern": "^[a-z][a-z0-9_.-]*/v[0-9]+$",
            "examples": ["com.solosoul.address/v1"]
          },
          "version": {
            "type": "integer",
            "description": "契约主版本号（描述性，host 不强制 gate）",
            "minimum": 1,
            "default": 1
          },
          "displayName": {
            "type": "string",
            "description": "UI 显示名",
            "maxLength": 64
          }
        }
      }
    },
    "field_bindings": {
      "type": "array",
      "description": "插件显式需要的 contract 字段。typed-lookup 时 host 验证访问权限。",
      "items": {
        "type": "object",
        "required": ["contractTypeId", "propertyId"],
        "properties": {
          "contractTypeId": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_.-]*/v[0-9]+$"
          },
          "propertyId": {
            "type": "string",
            "description": "UserTemplate.properties[].id"
          },
          "abiName": {
            "type": "string",
            "description": "Wasm 侧的 getter 名字，如 'address.get_country'"
          },
          "sensitivity": {
            "type": "string",
            "enum": ["public", "internal", "sensitive", "critical"]
          }
        }
      }
    }
  },
  "additionalProperties": false
}
